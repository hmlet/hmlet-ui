# Hoist class-variance-authority to fix TS2742 declaration emit errors
# when building in pnpm's isolated environments (e.g., git-hosted installs)
public-hoist-pattern[]=class-variance-authority
public-hoist-pattern[]=class-variance-authority/*
